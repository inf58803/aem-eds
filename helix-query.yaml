indices:
  english: &default
    include:
      - /**
    exclude:
      - /snippets/**
    target: /query-index.json
    properties:
      title:
        select: meta[name="title"]
        value: |
         attribute(el, 'content')
      description: 
        select: meta[name="description"]
        value: | 
          attribute(el, 'content')
